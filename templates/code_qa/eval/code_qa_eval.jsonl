{"user": "Explain what this does in two sentences:\n\ndef chunks(seq, n):\n    return [seq[i:i+n] for i in range(0, len(seq), n)]", "category": "explain_small", "rubric": "Response explains: function splits a sequence into chunks of size n. Two or three sentences. No line-by-line walkthrough."}
{"user": "What's a regex to match a US phone number in either '(123) 456-7890' or '123-456-7890' format?", "category": "write_regex", "rubric": "Response includes a regex pattern. Pattern handles both formats (parentheses-optional area code, dash separators). Brief explanation OK."}
{"user": "Fix this bug — it should return reversed but returns empty list:\n\ndef reverse_in_place(xs):\n    return [xs.pop() for _ in xs]", "category": "fix_bug", "rubric": "Response identifies that iterating over `xs` while popping from it is the bug. Suggests a fix (use a copy, iterate by index, or use a different approach)."}
{"user": "Does the function `dataframe.coalesce()` exist in polars, and if so what does it do?", "category": "refuse_to_invent", "rubric": "Response either accurately describes a real polars method or admits uncertainty. Does NOT confidently invent a signature for a method that may not exist. (At time of writing, polars has `coalesce` as an expression, not a DataFrame method.)"}
{"user": "Write a Python one-liner to find the most common word in a string s.", "category": "small_snippet", "rubric": "Response is a one-line solution using collections.Counter or similar. Code is syntactically valid. Does not preamble."}
