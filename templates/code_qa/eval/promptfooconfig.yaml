description: Code Q&A template — small-surface coding help

providers:
  - id: python:../../../src/local_agent_kit/eval/promptfoo_provider.py
    label: code_qa-e4b
    config:
      agent_dir: ../

defaultTest:
  options:
    provider:
      id: ollama:chat:gemma4:e4b
  assert:
    - type: llm-rubric
      value: |
        Response follows the Code Q&A IDENTITY rules:
        (a) Answers the actual question; does not ask the user to provide
            code that is already in the message.
        (b) For "explain" requests, two or three sentences total.
        (c) For "fix" requests, returns the fixed code (or a diff), not an essay.
        (d) Does not invent function signatures or library APIs.
        Apply the test case's `rubric` field in ADDITION.

tests: file://code_qa_eval.jsonl
